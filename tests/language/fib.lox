fun fib(n) {
  if (n < 2) return n;

  return fib(n - 2) + fib(n - 1);
}

var start = clock(); // returns elapsed time since the program started running, in seconds
print start;

print fib(35);
print clock() - start;